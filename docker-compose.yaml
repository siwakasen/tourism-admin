services:
  admin-service:
    image: $CI_REGISTRY_IMAGE:latest
    container_name: admin-service
    restart: unless-stopped
    environment:
      - ENV=production
    networks:
      - nginx-network
networks:
  nginx-network:
    external: true
